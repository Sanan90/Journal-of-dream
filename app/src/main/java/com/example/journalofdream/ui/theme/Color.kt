package com.example.journalofdream.ui.theme

import androidx.compose.ui.graphics.Color

// Основная палитра приложения
val DeepNavy       = Color(0xFF000033)   // Тёмно-синий фон (верх градиента)
val DeepPurple     = Color(0xFF330066)   // Фиолетовый фон (низ градиента)

// Кнопки главного экрана
val DreamButtonStart   = Color(0xFF1A237E) // "Записать сновидение" — начало градиента
val DreamButtonEnd     = Color(0xFF283593) // "Записать сновидение" — конец градиента
val LocationButtonStart = Color(0xFF4A148C) // "Локации" — начало градиента
val LocationButtonEnd   = Color(0xFF6A1B9A) // "Локации" — конец градиента
val TechButtonStart    = Color(0xFF1B5E20)  // "Техники" — начало градиента
val TechButtonEnd      = Color(0xFF2E7D32)  // "Техники" — конец градиента

// Общие UI цвета
val OverlayLight   = Color(0x33FFFFFF)   // Белый с 20% прозрачностью (фон полей)
val OverlayDark    = Color(0x80000000)   // Чёрный с 50% прозрачностью (фон BasicTextField)

// Material3 палитра
val Purple80       = Color(0xFFD0BCFF)
val PurpleGrey80   = Color(0xFFCCC2DC)
val Pink80         = Color(0xFFEFB8C8)
val Purple40       = Color(0xFF6650a4)
val PurpleGrey40   = Color(0xFF625b71)
val Pink40         = Color(0xFF7D5260)

// Акценты темы
val NeonPurple     = Color(0xFFBB86FC)
val Gold           = Color(0xFFFFD700)
val DarkSurface    = Color(0xFF121212)
val CardSurface    = Color(0xFF1E1E1E)
